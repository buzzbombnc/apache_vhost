  ServerAdmin {{ vhost_admin }}
  ServerName {{ vhost_name }}
{% if vhost_aliases|default() %}
  ServerAlias {{ vhost_aliases|join(' ') }}
{% endif %}

{% if vhost_docroot|default() %}
  DocumentRoot "{{ vhost_docroot }}"

{% endif %}
